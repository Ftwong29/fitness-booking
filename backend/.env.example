# Rename this file to `.env` before running the project
# This file defines the environment variables required by the backend

# SQLite database path (default for local development)
DATABASE_URL="file:./dev.db"

# Secret used for encoding simple token-based auth
SIMPLE_SECRET="your_secret_here"
